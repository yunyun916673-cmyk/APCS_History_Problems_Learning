線段覆蓋長度
標籤 : APCS
通過比率: 5590人/ 6614人 ( 85%) [非即時]
評分方式： 
最近更新 : 2024-02-01 10:57

內容
APCS 201603-3 線段覆蓋長度

給定一維座標上一些線段，求這些線段所覆蓋的長度，注意，重疊的部分只能算一次。

例如給定 4 個線段：(5, 6)、(1, 2)、(4, 8)、(7, 9)，如下圖，線段覆蓋長度為 6 。



輸入說明
第一列是一個正整數 N ，表示此測資有 N 個線段。
接著的 N 列每一列是一個線段的開始端點座標整數值 L 和結束端點座標整數值 R ，開始端點座標值小於等於結束端點座標值，兩者之間以一個空格區隔。

 

其中  30%的測資滿足， N < 100 ， 0 ≤ L , R < 1000 ，並且線段沒有重疊。
其中  70%的測資滿足， N < 100 ， 0 ≤ L , R < 1000 ，並且線段可能重疊。
其中100%的測資滿足， N < 10000 ， 0 ≤ L , R < 10000000 ，並且線段可能重疊。


輸出說明
輸出其總覆蓋的長度。

本題為嚴格比對，請務必按照說明進行輸出。


範例輸入 #1
5
160 180
150 200
280 300
300 330
190 210
範例輸出 #1
110
範例輸入 #2
1
120 120
範例輸出 #2
0

# 線段覆蓋長度

## 題目想法

一開始看到這一題的時候，我下意識想到直接使用 bitset 來記錄每個位置是否被覆蓋。

但是仔細思考後，我發現這題處理的是線段，而不是單純的位置。如果真的使用 bitset，需要按照座標範圍建立陣列，當座標範圍很大的時候，整體效率反而會下降。

因此後來我決定改用 vector 儲存每一條線段的左界以及右界，直接處理線段之間的關係。

在解這題的過程中，我也透過查詢資料以及詢問問題，第一次接觸到區間處理相關的技巧。原本可能需要直接掃描所有位置的方法，可以透過排序以及合併區間的方式簡化。


## 解題過程

因為這題的座標範圍比較大，所以不能使用直接標記每個位置的方法。

首先我使用 PLL 代替 pair<ll,ll>，讓線段的左界與右界可以更方便表示。

進入主函式後，我先輸入 N，並將所有線段存入 all 中。

接著依照左界進行排序，讓所有線段按照順序排列。

排序完成後，我建立一個新的 vector tmp，用來存放目前已經合併完成的覆蓋區間。

首先初始化加入排序後最小左界的線段。

接著處理 all 中剩下的每一條線段。

我先使用變數 plus 儲存目前線段的長度，如果長度為 0，代表這條線段不會增加任何覆蓋範圍，因此直接 continue，不用進行後續操作。

接著取出 tmp 最後一項的左界與右界。

這裡我也遇到一個小錯誤。之前實作時，我只是複製出一份左界與右界的數值，而不是直接取得 tmp 中的元素，因此後續更新右界時並沒有真的修改到 tmp 裡面的資料。

修正後，我可以直接修改目前最後一條線段的右界。

接著判斷目前線段的左界：

如果目前線段的左界小於等於 tmp 最後一項的右界，代表兩條線段有重疊，因此將兩條線段合併，並更新最大的右界。

如果沒有重疊，就代表這是一個新的覆蓋區間，直接加入 tmp。

最後將 tmp 中每一條線段的長度累加起來，就是所有線段覆蓋的總長度。

## 解題方法思考

這題讓我了解到，處理線段問題時，不一定需要直接處理每一個座標位置。

一開始想到 bitset 是因為它可以直接表示位置是否被覆蓋，但受到座標範圍限制後，會發現它並不是最適合的方法。

後來改用排序以及區間合併，直接處理線段之間的關係。

排序後，只需要維護目前最後一個有效覆蓋區間，就可以判斷新的線段是否需要合併。

這樣原本可能需要大量掃描的位置處理方式，轉換成只需要處理線段數量的方式。

## 複雜度分析

### 時間複雜度

O(N log N)

原因是：

首先需要將所有線段依照左界排序，排序需要 O(N log N)。

排序完成後，只需要依序掃描每條線段並進行合併，這部分需要 O(N)。

因此總時間複雜度為：

O(N log N)

### 空間複雜度

O(N)

原因是：

需要使用 all 儲存所有輸入線段，並使用 tmp 儲存合併後的有效區間。

最多需要儲存 N 條線段，因此空間複雜度為：

O(N)

## 學習心得

透過這一題，我學習到了區間合併的演算法。

原本我可能會使用直接掃描每個位置的方法，但因為座標範圍較大，所以必須重新思考資料的處理方式。

這題也讓我了解到，很多問題不一定要按照題目的表面直接模擬，而是可以透過整理資料以及改變處理方式來降低複雜度。

原本可能需要接近平方等級的演算法，可以透過排序與區間合併簡化成 O(N log N)。

透過這一題，我也更加熟悉了線段問題的處理方式，並讓自己的解題能力更加充實。